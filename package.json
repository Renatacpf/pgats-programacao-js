{
  "name": "pgats-programacao",
  "version": "1.0.0",
  "description": "Este repositório contém scripts em JavaScript desenvolvidos durante a disciplina **Programação para Automação de Testes**, parte do curso de **Pós-Graduação em Automação de Testes de Software**.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "rm -rf mochawesome-report/ test-results/ && mkdir -p mochawesome-report test-results && mocha testes/**/*.js --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome.html,overwrite=true,json=false --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results/results.xml",
    "test:final": "rm -rf mochawesome-report/ test-results/ && mkdir -p mochawesome-report test-results && mocha testes/trabalho-final.js --reporter mochawesome --reporter-options reportDir=mochawesome-report,reportFilename=mochawesome.html,overwrite=true,json=false --reporter mocha-junit-reporter --reporter-options mochaFile=./test-results/results.xml"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Renatacpf/pgats-programacao-js.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Renatacpf/pgats-programacao-js/issues"
  },
  "homepage": "https://github.com/Renatacpf/pgats-programacao-js#readme",
  "devDependencies": {
    "mocha": "^11.2.2",
    "mocha-junit-reporter": "^2.2.1"
  }
}
